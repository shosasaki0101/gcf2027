server {
    listen 80;
    server_name your-domain.com;
    root /path/to/your/dist;
    index index.html;

    location /gcf/ {
        try_files $uri $uri/ /gcf/index.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
